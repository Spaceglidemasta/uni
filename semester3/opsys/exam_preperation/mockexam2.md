# Mock Exam
## 1
Wtwttwtwww
## 2
pwd
cat
crontab
sed
head
ls
sort
zip
rm / rmdir
echo
ln
chmod
## 3
needs to import \__future__

+ smaller page table
+ more internal frag

if space on memory > get
if space on hard disk > swap
else / if out of scope > segfault

negativly, but not much if req. space on memory. If the req space is on harddisk, 
this can take up to 100,000x longer. The worst cash is segfault, where the program crashes

```c
int main(void){
    int* px = NULL;
    int x = *px;

    return 0;
}
```

in the pcb

call ..

they are related (c & p) / this is true: floor(sizeof(doc1) / PAGE_SIZE) == floor(sizeof(doc2) / PAGE_SIZE)

process prio for the scheduler

scheduler

clones the program with the PPID_child becoming PID_parent

invokes a program upon the current one

process is a running instance of a program

when signal and wait overhead is too big / avg busy waiting intervals are very short

1

2

starvation / long runtime

2

gets blocked

named & anonymous

OS closes S_a

interact with kernel level code from user level

CPU switches to kernel mode, executes the code and then switches back

the interrupt is delayed till the ISR is done

negativly from overhead, but positivly from ISR handleling it immideatly

## 7

/home/linus/mono.c

../../bin/dash

/home/usr/bin/zsh
./bin/zsh

/media/usb/src/password_generator/gen.cpp

